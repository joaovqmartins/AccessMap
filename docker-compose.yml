services:
  postgres:
    image: postgres:16
    container_name: accessmap-db
    environment:
      POSTGRES_DB: accessmap
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: accessmap123
    ports:
      - "5432:5432"